/*
this code could be used as part of the function that
gets the Selected Name from the dropdown list
and add it to the store :

return (mobXGlobalStore.users).find(function(i) {
    return i.name === nameToCheck;
});
 */